import { wasteFormService } from '../wasteFormService';

describe('wasteFormService', () => {
  it('should fetch waste forms for a subsidiary', async () => {
    // TODO: mock supabaseClient and test getWasteForms
    expect(true).toBe(true);
  });

  it('should create a new waste form', async () => {
    // TODO: mock supabaseClient and test createWasteForm
    expect(true).toBe(true);
  });

  it('should update a waste form', async () => {
    // TODO: mock supabaseClient and test updateWasteForm
    expect(true).toBe(true);
  });

  it('should delete a waste form', async () => {
    // TODO: mock supabaseClient and test deleteWasteForm
    expect(true).toBe(true);
  });
});
