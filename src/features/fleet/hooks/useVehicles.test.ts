import { useVehicles } from '../hooks/useVehicles';

describe('useVehicles', () => {
  it('should fetch vehicles for a subsidiary', () => {
    // TODO: mock react-query and vehicleService
    expect(true).toBe(true);
  });
});
